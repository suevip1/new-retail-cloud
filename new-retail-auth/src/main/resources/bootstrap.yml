server:
  port: 4000
  servlet:
    context-path: /auth
spring:
  application:
    name: new-retail-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 24
        max-idle: 24
        min-idle: 12
my:
  name: zhangsan
  age: 20
